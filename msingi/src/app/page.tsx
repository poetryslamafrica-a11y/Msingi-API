// Next.js App Router requires this file to be named "page.tsx" to be
// recognised as the homepage route — that naming is fixed by the framework
// and can't be changed. The actual homepage content lives in ./index.tsx;
// this file just re-exports it so the route resolves correctly.
export { default } from "./index";
